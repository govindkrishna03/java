import gui.LoginPanel;

public class Main {
    @SuppressWarnings("deprecation")
    public static void main(String[] args) {
        LoginPanel loginPanel = new LoginPanel();
        loginPanel.show(); // Assuming your LoginPanel class has a method to display the GUI
    }
}
